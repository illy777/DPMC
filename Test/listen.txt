# Schrauben = []
# Stator = []
# Rotor = []
# Gehaeuse = []
# Pumpengehaeuse = []
# Deckel = []
# Staubschutz = []
# Schutzhuelse = []
# Oring = []
# Anlaufscheibe = []
# Kuehlblech = []
# Lot = []
# Schmierstoff = []


#for keys in Pumpe0:
    # if "Schraube" in str(keys):
    #     Schrauben.append(Pumpe0[keys])
    # if "Stator" in str(keys):
    #     Stator.append(Pumpe0[keys])
    # if "Rotor" in str(keys):
    #     Rotor.append(Pumpe0[keys])
    # if "Gehaeuse" in str(keys)[5:]:
    #     Gehaeuse.append(Pumpe0[keys])   
    # if "Pumpengehaeuse" in str(keys):
    #     Pumpengehaeuse.append(Pumpe0[keys])
    # if "Deckel" in str(Pumpe0[keys]):
    #     Deckel.append(Pumpe0[keys])
    # if 

    # for keys in Pumpe1:
    #     liste = Pumpe1[keys]
    #     x=0
    #     for element in Schrauben:
    #         if element[1] == liste[1]:
    #             Schrauben[x][0] = element[0]+liste[0]
    #         else:
    #             x = x+1
    #     for element in Stator:
    #         if element[1] == liste[1]:
    #             Schrauben[x][0] = element[0]+liste[0]
    #         else:
    #             x = x+1